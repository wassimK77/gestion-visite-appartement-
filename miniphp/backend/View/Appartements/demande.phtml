<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Demande de Location</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .container {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 400px;
        }
        h1 {
            color: #333;
            text-align: center;
            margin-bottom: 20px;
        }
        p {
            color: #666;
            margin-bottom: 20px;
        }
        input[type="text"], input[type="date"], input[type="email"], input[type="number"] {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
        }
        button {
            background-color: #007BFF;
            color: #fff;
            padding: 10px 15px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            width: 100%;
            margin-top: 20px;
        }
        button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Demande de Location</h1>
        <p>Vous souhaitez devenir locataire de cet appartement ? Remplissez le formulaire ci-dessous :</p>
        <form action="<?php echo Util_Route::url('demande', 'appartement', ['id' => (string)$this->appartement->NUMAPPART]); ?>" method="POST">
            <input type="hidden" name="client_id" value="<?php echo $this->client->id; ?>">
            <input type="text" name="nom" value="<?php echo $this->client->NOM_CLI; ?>" placeholder="Nom" readonly>
            <input type="text" name="prenom" value="<?php echo $this->client->PRENOM_CLI; ?>" placeholder="Prénom" readonly>
            <input type="text" name="tel" value="<?php echo $this->client->TEL_CLI; ?>" placeholder="Téléphone" readonly>
            <button type="submit">Faire une demande de location</button>
        </form>
    </div>
</body>
</html>